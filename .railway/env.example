# Backend Service Environment Variables

# Database - Railway provides SQLite volume
DATABASE_URL=file:./prisma/production.db

# JWT Secret - Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environment
NODE_ENV=production

# Backend Port (Railway auto-assigns if not specified)
PORT=3001

# Frontend Service Environment Variables

# Backend API URL - Replace with actual Railway backend URL after deployment
# Example: https://aturrail-backend.railway.app
VITE_API_URL=https://your-backend-service.railway.app
